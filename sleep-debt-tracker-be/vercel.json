{
  "version": 2,
  "builds": [
    {
      "src": "server.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["server.ts", "dist/**/*", "node_modules/**/*"]
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "server.ts" }
  ]
}
